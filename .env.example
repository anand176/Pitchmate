# Local dev: use your Gemini key here (only for npm run dev)
# Get your key from https://aistudio.google.com/apikey
VITE_GEMINI_API_KEY=your_gemini_api_key_here

# Production (e.g. Vercel): set this so the app uses /api/chat proxy instead of the key in the browser
# VITE_CHAT_ENDPOINT=/api/chat

# Supabase (auth & database)
# Get URL and anon key from your Supabase project settings
VITE_SUPABASE_URL=your_supabase_url_here
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key_here

# Pinecone (knowledge base for Agentic flow)
# Create an index at https://app.pinecone.io with dimension 768 (text-embedding-004)
PINECONE_API_KEY=your_pinecone_api_key_here
PINECONE_INDEX=pitchmate
